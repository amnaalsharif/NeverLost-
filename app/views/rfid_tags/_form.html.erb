<%= form_for(@rfid_tag) do |f| %>
  <% if @rfid_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rfid_tag.errors.count, "error") %> prohibited this rfid_tag from being saved:</h2>

      <ul>
      <% @rfid_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.number_field :tag_id %><br>

    <%= f.label :status %><br>
    <p>Is the tag being used? Only check box if tag being used</p>
    <p> yes <%= f.check_box :status%></p>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
